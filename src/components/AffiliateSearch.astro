---
import { amazonSearchUrl } from "../lib/site";
import AmazonLink from "./AmazonLink.astro";

interface Props {
  query: string;
  label?: string;
}

const { query, label = query } = Astro.props;
const href = amazonSearchUrl(query);
---

<p>
  <AmazonLink href={href}>{label}</AmazonLink>
</p>
