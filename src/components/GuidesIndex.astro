---
import { publishedSearchIndex } from "../lib/guides-index";

const index = await publishedSearchIndex();
const json = JSON.stringify(index).replaceAll("<", "\\u003c");
---

<script is:inline id="guides-index" type="application/json" set:html={json} />
